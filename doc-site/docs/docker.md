# Docker documentation

Choose the guide that matches what you want to do:

- **[Docker install (Compose)](docker-install.md)** — default **`docker-compose.yml`**, **`./.env`**, persisted MySQL/`config.php`, GHCR pulls, screenshots, production-minded notes.
- **[Docker development & CI](docker-develop.md)** — **`docker-compose.dev.yml`**, E2E/Playwright, PHPUnit, **`web`** env reference, maintainer GHCR publishes, GitHub Actions.

In-repo **`AGENTS.md`** is optimized for Cursor / IDE contributor notes beside the codebase. Detailed install steps: **`docker/README.md`**.
